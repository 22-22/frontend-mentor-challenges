import { alata } from "../../fonts";

export default function Logo() {
    return (
        <div className={`${alata.className} font-bold text-4xl text-white leading-tight`}>loopstudios</div>
    )
}
